{
  "name": "supadump",
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "lint": "tsc --noEmit",
    "start": "bun run index.ts",
    "format": "prettier --write .",
    "build": "bun run lint && bun build --compile --minify --outfile dist/supadump ./index.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@kitajs/ts-html-plugin": "^4.1.3",
    "@types/bun": "latest",
    "knip": "^5.64.2",
    "prettier": "^3.4.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@commander-js/extra-typings": "^14.0.0",
    "@supabase/supabase-js": "^2.74.0",
    "commander": "^14.0.1",
    "consola": "^3.4.2",
    "picocolors": "^1.1.1"
  }
}
